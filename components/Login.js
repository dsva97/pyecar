import React from "react";
import { Input } from "@chakra-ui/react";

const Login = () => {
  return (
    <div>
      <Input placeholder="Choose your username" />
      <Input type="file" placeholder="Choose your username" />
    </div>
  );
};

export default Login;
